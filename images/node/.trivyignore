# .trivyignore — Node image
# Format: CVE-ID  [expiry-date]  # reason
#
# Add accepted/deferred CVEs here with a justification comment and optional
# expiry date (YYYY-MM-DD). Entries without an expiry must be reviewed
# quarterly. Never suppress CRITICAL CVEs without an approved exception.

# zlib1g — Debian 12 "will_not_fix": the affected code path (zipOpenNewFileInZip4_6)
# is only reachable when creating ZIP archives. Node.js does not call this
# function at runtime; the vulnerability is not exploitable in this container.
CVE-2023-45853

# libc6 / libc-bin — glibc memalign integer overflow (no fix released for Debian 12
# as of 2026-03-20). Not exposed over the network in this container.
CVE-2026-0861
