# Generated at runtime by make scan / make sbom — never commit scan results
reports/

# node_modules from local dev (node image has its own inside Docker)
node_modules/

# Python bytecode
__pycache__/
*.py[cod]

# Editor / OS noise
.DS_Store
.idea/
.vscode/
*.swp
*.swo
Thumbs.db

# Local-only Claude state (project-shared config stays committed)
.claude/settings.local.json
CLAUDE.local.md
.claude/worktrees/
